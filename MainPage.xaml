<Page
    x:Class="EldenTracker.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EldenTracker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid>
            <ScrollViewer x:Name="scrollViewer" ManipulationMode="All" ZoomMode="Enabled" HorizontalScrollMode="Enabled" VerticalScrollMode="Enabled" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Image x:Name="mapImage" Source="Resources/Map/Images/FullMap.png" Stretch="Uniform"/>
            </ScrollViewer>
        </Grid>
        <Grid>
            <Grid x:Name="LeftPanelGrid" Visibility="Collapsed" HorizontalAlignment="Left" Background="Black" Width="500">
                <!-- Your left panel content here -->
            </Grid>
            <Grid x:Name="ButtonGrid" Margin="0, 40, 0, 0" HorizontalAlignment="Left" VerticalAlignment="Top">
                <Button x:Name="ToggleGridButton" Content="Show Grid" Click="ToggleGridButton_Click" Background="Black"/>
            </Grid>
        </Grid>
    </Grid>
</Page>